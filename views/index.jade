extends layout

block content

    form(name="actionAdd", action="/actions/create", method="post")
        input(type="text", name="actionName", align="left", placeholder="what do you like doing?")
        //- input(type="submit", name="addActionSubmit", placeholder="what do you like doing?", class="btn btn-default")